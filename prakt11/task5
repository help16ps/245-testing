import random

ball_question = input("WHAT IS YOUR NAME: ")

print("Привет,", ball_question, ",я магический шар, и я знаю ответ на любой твой вопрос.")

asking_question = input("Какой у вас вопрос?: ")

answered = False

while True:

    if asking_question == "":
        print("u wrote nothing lmao")
    else:
        category_rng = random.randint(1, 4)

        # positive

        if category_rng == 1:


            answer_rng = random.randint(1, 5)

            if answer_rng == 1:
                print("Бесспорно")
                print()
                # answered = True

            if answer_rng == 2:
                print("Предрешено")
                print()
                # answered = True

            if answer_rng == 3:
                print("Никаких сомнений")
                print()
                # answered = True

            if answer_rng == 4:
                print("Определённо да")
                print()
                # answered = True

            if answer_rng == 5:
                print("Можешь быть уверен в этом")
                print()
                # answered = True

        # partially positive

        if category_rng == 2:

            answer_rng = random.randint(1, 5)

            if answer_rng == 1:
                print("Мне кажется - да")
                print()
                # answered = True

            if answer_rng == 2:
                print("Вероятнее всего")
                print()
                # answered = True

            if answer_rng == 3:
                print("Хорошие перспективы")
                print()
                # answered = True

            if answer_rng == 4:
                print("Знаки говорят - да")
                print()
                # answered = True

            if answer_rng == 5:
                print("Да")
                print()
                # answered = True

        # neutral

        if category_rng == 3:

            answer_rng = random.randint(1, 5)

            if answer_rng == 1:
                print("Пока неясно, попробуй снова")
                print()
                # answered = True

            if answer_rng == 2:
                print("Спроси позже")
                print()
                # answered = True

            if answer_rng == 3:
                print("Лучше не рассказывать")
                print()
                # answered = True

            if answer_rng == 4:
                print("Сейчас нельзя предсказать")
                print()
                # answered = True

            if answer_rng == 5:
                print("Сконцентрируйся и спроси опять")
                print()
                # answered = True

        # negative

        if category_rng == 4:

            answer_rng = random.randint(1, 5)

            if answer_rng == 1:
                print("Даже не думай")
                print()
                # answered = True


            if answer_rng == 2:
                print("Мой ответ - нет")
                print()
                # answered = True

            if answer_rng == 3:
                print("По моим данным - нет")
                print()
                # answered = True

            if answer_rng == 4:
                print("Перспективы не очень хорошие")
                print()
                # answered = True

            if answer_rng == 5:
                print("Весьма сомнительно")
                print()
                # answered = True

    while True:
        asking_question = int(input("Хотите спросить ещё вопрос?        1. Да  0. Нет "))

        if asking_question == 1:

            # answered = False
            print()
            asking_question = input("Спросите ещё вопрос: ")

        # more while trues hooray

        if asking_question == 0:
            print("выходим...")
            print()
            break

        else:
            print("ошибка, неправильно введен ответ")
            print()
    break
